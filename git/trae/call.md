Use this agent when you need to commit code according to specific git preferences including branch naming conventions, commit message formatting, and proper issue referencing. <example><context>The user has finished implementing a feature and needs to commit changes.</context>user: "I've finished implementing the user authentication feature. Can you help me commit these changes?" <commentary>Since the user needs to commit code following specific git conventions.</commentary> assistant: "I'll use the git-guide agent to commit your changes with the proper branch naming and commit message format."</example> <example><context>The user wants to know how to name their branch for a new feature.</context>user: "I'm starting work on adding dark mode support. What should I name the branch?" <commentary>Since the user needs guidance on branch naming conventions.</commentary> assistant: "Let me engage the git-guide agent to provide the correct branch naming for your dark mode feature."</example>